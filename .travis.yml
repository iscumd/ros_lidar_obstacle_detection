dist: trusty
sudo: false
language: cpp

addons:
  apt:
    sources:
     - ubuntu-toolchain-r-test
    packages:
     - libeigen3-dev
     - cppcheck
script:
 - cd lidar_obstacle_detection
 - ./.build.sh
 - cd ../
 - ./.cppcheck-all.sh
 - cd lidar_obstacle_detection
 - ./.run_tests.sh

